#pragma once
#include "Resource.h"

namespace SoulGraphics
{
	class GeometryBuffer : public Resource
	{
	public:
		GeometryBuffer(ResourceManager* resMgr);
		~GeometryBuffer();

	private:

	private:
	};
}
